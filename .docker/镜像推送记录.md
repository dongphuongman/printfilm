# 镜像推送记录

## 2026-07-17

| 服务 | 镜像 | Digest | 标签 |
|------|------|--------|------|
| api | `gcc-registry.cn-hangzhou.cr.aliyuncs.com/gcc/ai-manju-api` | `sha256:51d2410290cc236aa8bcf490eba1a16c20d705d8af195f6e2aaa2c59edb579d3` | latest |
| web | `gcc-registry.cn-hangzhou.cr.aliyuncs.com/gcc/ai-manju-web` | `sha256:0a33cdbbb0c4fd3149ef53301febdb838cdf9d22a6c97e0e6598c5438fe2c2cc` | latest |

**仓库：** gcc-registry.cn-hangzhou.cr.aliyuncs.com/gcc  
**Compose：** docker-compose.full.yml  
**说明：** 首次补齐 Dockerfile 与 compose 全栈配置后构建推送；Postgres/Redis 仍使用公共镜像，未推送。

### 服务器拉取示例

```bash
docker pull gcc-registry.cn-hangzhou.cr.aliyuncs.com/gcc/ai-manju-api:latest
docker pull gcc-registry.cn-hangzhou.cr.aliyuncs.com/gcc/ai-manju-web:latest
```

### 部署提示

- 生产环境需设置 `JWT_SECRET`、`TOKENFREE_API_KEY`、`ARK_API_KEY` 等环境变量
- 前端构建参数 `NEXT_PUBLIC_API_URL` 需指向浏览器可访问的 API 地址
- 中间件可使用 compose 内 postgres/redis，或外部托管实例
